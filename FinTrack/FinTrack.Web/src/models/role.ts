export enum Role {
  Admin = 'Admin',
  User = 'User',
}

export enum HouseholdMemberRole {
  Member,
  Admin,
  Owner,
}

export enum HouseholdMemberStatus {
  PendingRespone,
  Expired,
  Rejected,
  Active,
}
